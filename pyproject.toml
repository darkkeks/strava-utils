[project]
name = "strava-utils"
version = "0.1.0"
description = "Utilities for Strava automation."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27.0",
]

[project.scripts]
strava-monitor = "strava_monitor:main"

[tool.uv]
package = false
